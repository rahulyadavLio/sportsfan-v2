/**
 * @deprecated This file has been replaced by the backend API.
 * All record data is now served from Firestore via:
 *   GET /records?event=&category=
 *   GET /records/trends?event=
 *   GET /records/progress?event=
 *   GET /records/stories
 *
 * Use the hooks in src/hooks/useRecords.ts instead.
 */